{
  "routes": [
    { "src": "/static/(.*)", "dest": "/static/$1" },
    { "src": "/", "dest": "/templates/dashboard.html" },
    { "src": "/search/(.*)", "dest": "/templates/search_results.html" },
    { "src": "/results", "dest": "/templates/search_results.html" }
  ],
  "headers": [
    {
      "source": "/(.*)",
      "headers": [
        {
          "key": "Cache-Control",
          "value": "public, max-age=0, must-revalidate"
        }
      ]
    }
  ]
}

